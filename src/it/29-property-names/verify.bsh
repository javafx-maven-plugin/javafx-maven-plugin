import java.io.*;

File barFolder = new File( basedir, "target/bar" );
if( !barFolder.exists() ){
    throw new Exception( "there should be a bar-folder!");
}

File fooJar = new File( barFolder, "foo.jar" );
if( !fooJar.exists() ){
    throw new Exception( "there should be a foo.jar file!");
}